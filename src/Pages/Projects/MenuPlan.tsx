import { useEffect } from "react";

const MenuPlan = () => {
 useEffect(() => {
  window.scrollTo(0, 0);
 }, []);

 return (
  <div style={{ margin: "1200px 0" }}>
   <h1>HELO</h1>MenuPlan
  </div>
 );
};

export default MenuPlan;
